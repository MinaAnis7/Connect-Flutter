import 'package:flutter/material.dart';

class MyConnections extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("My Connections"),),
    );
  }
}
